export default function AdminSettings() {
  return (
    <div>
      <h1 style={{ fontSize: 28 }}>Settings</h1>
      <p style={{ marginTop: 10, maxWidth: 480 }}>
        Placeholder for v1. Reserved for future admin-profile management, WhatsApp number configuration, and
        delivery-zone editing directly from the dashboard instead of code.
      </p>
    </div>
  );
}
